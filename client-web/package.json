{
  "private": true,
  "scripts": {
    "start": "vite",
    "watch": "rsw watch",
    "build": "wasm-pack build core --out-dir pkg --target web --release && tsc --noEmit && vite build",
    "test": "wasm-pack build core --out-dir pkg --target nodejs --release && VITE_API_HOST=https://api.qqself.com vitest --run --test-timeout 10000",
    "format": "prettier --write 'src/**/*.ts'",
    "format:check": "prettier --check 'src/**/*.ts'"
  },
  "dependencies": {
    "lit": "^2.4.0"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.0.4",
    "vite": "^4.3.8",
    "vite-plugin-rsw": "^2.0.11",
    "vitest": "^0.31.1"
  }
}
