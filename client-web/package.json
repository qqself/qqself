{
  "private": true,
  "scripts": {
    "start": "vite",
    "watch": "rsw watch",
    "build": "wasm-pack build core --out-dir pkg --target web --release && vite build"
  },
  "dependencies": {
    "lit": "^2.4.0",
    "vite-plugin-rsw": "^2.0.9"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "vite": "^3.1.8"
  }
}
