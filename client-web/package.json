{
  "private": true,
  "scripts": {
    "start": "vite",
    "watch": "rsw watch",
    "build": "wasm-pack build core --out-dir pkg --target web --release && tsc --noEmit && vite build",
    "test": "wasm-pack build core --out-dir pkg --target nodejs --release && jest",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "lit": "^2.4.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4",
    "vite": "^4.3.8",
    "vite-plugin-rsw": "^2.0.11"
  }
}
